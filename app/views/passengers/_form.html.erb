<%= form_for(@passenger) do |f| %>
  <% if @passenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passenger.errors.count, "error") %> prohibited this passenger from being saved:</h2>

      <ul>
      <% @passenger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :car_id %><br>
    <%= f.select :car_id, options_for_select(@cars) %>
  </div>
  <div class="field">
    <%= f.label :destination %><br>
    <%= f.select :destination, options_for_select(@floors) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
